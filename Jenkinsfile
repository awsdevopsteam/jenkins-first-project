pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                echo 'come to clarusway'
                sh 'echo Hello World'
            }
        }
    }
}
